name: demo_server
description: A sample Dart server fixture.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  dart_frog: ^1.1.0

dev_dependencies:
  very_good_analysis: ^6.0.0
  test: ^1.25.0
